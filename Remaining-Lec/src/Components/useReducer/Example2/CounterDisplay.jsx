

const CounterDisplay = ({count}) => {
    console.log(count);
    return (
        <div>
            <h2>Counter: {"i am also here"}</h2>
            <h2>{count}</h2>
        </div>
    );
};

export default CounterDisplay;
